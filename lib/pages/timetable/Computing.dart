import 'package:flutter/material.dart';

class COMPUTING extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Computing Faculty'),
      ),
      body: Center(
        child: Text('Computing Faculty Screen Content'),
      ),
    );
  }
}
