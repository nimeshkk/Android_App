import 'package:flutter/material.dart';

class ENGINEERING extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Engineering Faculty'),
      ),
      body: Center(
        child: Text('Engineering Faculty Screen Content'),
      ),
    );
  }
}
