import 'package:flutter/material.dart';

class Aiesec extends StatelessWidget {
  const Aiesec({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(child: Text('Aiesec')),
    );
  }
}
