import 'package:flutter/material.dart';

class IEEE extends StatelessWidget {
  const IEEE({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(child: Text('IEEE')),
    );
  }
}
