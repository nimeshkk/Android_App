import 'package:flutter/material.dart';

class Drama extends StatelessWidget {
  const Drama({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(child: Text('Drama')),
    );
  }
}
