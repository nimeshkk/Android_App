import 'package:flutter/material.dart';

class StemUp extends StatelessWidget {
  const StemUp({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(child: Text('Stem Up')),
    );
  }
}
