import 'package:flutter/material.dart';

class CSSE extends StatelessWidget {
  const CSSE({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(child: Text('CSSE')),
    );
  }
}
