import 'package:flutter/material.dart';

class FOSS extends StatelessWidget {
  const FOSS({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(child: Text('FoSS')),
    );
  }
}
